use std::collections::{HashSet, HashMap};

use fhq_treap::{TreapMap, TreapSet};

fn main() {
    // let mut v: TreapMap<i32, i32> = [(2, 3), (5, 4), (4, 3)].into_iter().collect();
    let mut p1: HashSet<i32> = Default::default();
    let mut p2: TreapSet<i32> = Default::default();
}
